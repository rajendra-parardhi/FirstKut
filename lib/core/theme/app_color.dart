
import 'package:flutter/material.dart';
class AppColors {
  // Backgrounds
  static const scaffoldBg = Color(0xffFAFAFA); // light gray
  static const white = Color(0xffFFFFFF);
  static const lightBg = Color(0xffF5F5F5);
  static const folderBg = Color(0xffF8F9FA);
  static const editorBg = Color(0xff0F172A); // dark editor

  // 🔥 Remove gradients → use flat colors
  static const gradientStart = Color(0xff000000);
  static const gradientEnd = Color(0xff000000);

  // Primary (Black theme)
  static const primary = Color(0xff000000);
  static const primaryLight = Color(0xff1F2937);

  // Buttons (shadcn style)
  static const buttonPrimary = Color(0xff000000);
  static const buttonSecondary = Color(0xffFFFFFF);

  // Text
  static const textPrimary = Color(0xff111827); // near black
  static const textSecondary = Color(0xff6B7280); // gray
  static const textWhite = Color(0xffFFFFFF);

  // Borders
  static const border = Color(0xffE5E7EB);
  static const borderDark = Color(0xffD1D5DB);

  // Shadows
  static const shadowLight = Color(0x0F000000); // very light
  static const shadowDark = Color(0x1A000000);

  // Misc
  static const overlayDark = Color(0x66000000);
}
// class AppColors {
//   // Backgrounds
//   static const scaffoldBg = Color(0xffF4F6FA);
//   static const white = Colors.white;
//   static const lightBg = Color(0xffF3F4F8);
//   static const folderBg = Color(0xffF9FAFF);
//   static const editorBg = Color(0xff1E1E1E);
//
//   // Gradients
//   static const gradientStart = Color(0xff5B2EFF);
//   static const gradientEnd = Color(0xff8F6CFF);
//
//   static const blue = Colors.blue;
//   static const blueAccent = Colors.blueAccent;
//   static const deepPurple = Colors.deepPurple;
//   static const purpleAccent = Colors.purpleAccent;
//
//   static const black87 = Colors.black87;
//   static const black54 = Colors.black54;
//
//   // Text & Icons
//   static const textWhite = Colors.white;
//   static const textGrey = Colors.grey;
//
//   // Borders & Shadows
//   static const borderLight = Colors.white24;
//   static const shadowLight = Colors.black12;
//   static const shadowDark = Colors.black26;
//
//   // Misc
//   static const overlayDark = Colors.black45;
// }